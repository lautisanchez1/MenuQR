# Database
quarkus.datasource.db-kind=postgresql
quarkus.datasource.jdbc.url=${DB_URL:jdbc:postgresql://localhost:5432/menudigital}
quarkus.datasource.username=${DB_USER:menudigital}
quarkus.datasource.password=${DB_PASS:menudigital}

# Flyway
quarkus.flyway.migrate-at-start=true
quarkus.flyway.locations=db/migration

# Hibernate
quarkus.hibernate-orm.database.generation=none
quarkus.hibernate-orm.log.sql=false

# CORS
quarkus.http.cors=true
quarkus.http.cors.origins=/.*/
quarkus.http.cors.methods=GET,POST,PUT,PATCH,DELETE,OPTIONS
quarkus.http.cors.headers=Content-Type,Authorization
quarkus.http.cors.access-control-allow-credentials=true

# Cognito (federated identity). Empty values disable verification.
cognito.issuer-url=${COGNITO_ISSUER_URL:}
cognito.client-id=${COGNITO_CLIENT_ID:}

# JWT Security — protected endpoints validate Cognito access tokens against the
# pool's JWKS. Audience is intentionally unset: Cognito access tokens carry
# `client_id` instead of `aud`. The fallback `https://example.invalid` prevents
# startup failure when Cognito env vars are missing; protected endpoints will
# simply reject all tokens until COGNITO_ISSUER_URL is set.
mp.jwt.verify.publickey.location=${COGNITO_ISSUER_URL:https://example.invalid}/.well-known/jwks.json
mp.jwt.verify.issuer=${COGNITO_ISSUER_URL:https://example.invalid}

# AWS S3
aws.s3.bucket=${S3_BUCKET:menudigital-images}
aws.s3.region=${AWS_REGION:us-east-1}
aws.s3.endpoint-override=${S3_ENDPOINT:}
aws.s3.public-url=${S3_PUBLIC_URL:# Solo MinIO / S3 con endpoint custom (local). En AWS real no uses S3_ENDPOINT ? DefaultCredentialsProvider (rol IAM).
aws.s3.access-key=${AWS_ACCESS_KEY_ID:local}
aws.s3.secret-key=${AWS_SECRET_ACCESS_KEY:local}

# AWS DynamoDB (en AWS con rol IAM: las claves se obtienen del IAM role si no se definen aqu�)
aws.dynamodb.table=${DYNAMO_TABLE:menudigital-events}
# Misma tabla que aws.dynamodb.table (AnalyticsService / repositorio legacy)
dynamodb.events.table=${DYNAMO_TABLE:menudigital-events}
aws.dynamodb.region=${AWS_REGION:us-east-1}
aws.dynamodb.endpoint-override=${DYNAMO_ENDPOINT:}
aws.dynamodb.access-key=${AWS_ACCESS_KEY_ID:}
aws.dynamodb.secret-key=${AWS_SECRET_ACCESS_KEY:}

# Health
quarkus.smallrye-health.root-path=/q/health

# OpenAPI / Swagger
quarkus.smallrye-openapi.path=/q/openapi
quarkus.swagger-ui.path=/q/swagger-ui
quarkus.swagger-ui.always-include=true
quarkus.smallrye-openapi.info-title=MenuDigital API
quarkus.smallrye-openapi.info-version=1.0.0
quarkus.smallrye-openapi.info-description=Multi-tenant digital menu SaaS platform API

# Logging
quarkus.log.level=INFO
quarkus.log.category."com.menudigital".level=DEBUG

# HTTP port
quarkus.http.port=8080

# Recomendaciones: artefacto opcional en S3 (ONNX, etc.); vac?o = no descargar. La l?gica sigue siendo mock hasta integrar inferencia.
recommendations.model.s3.bucket=${RECOMMENDATIONS_MODEL_S3_BUCKET:}
recommendations.model.s3.key=${RECOMMENDATIONS_MODEL_S3_KEY:}
